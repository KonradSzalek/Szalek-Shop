@model szalkszop.ViewModels.ProductListViewModel
@{
	ViewBag.Title = "Products";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.ProductList.FirstOrDefault().ProductCategory.Name - products</h2>

<br />

@foreach (var product in Model.ProductList)
{
	<div class="vert-offset-bottom-1 col-sm-3">
		<div class="thumbnail" style="height:380px ; position:relative;">
			@if (product.Images.Any())
			{
				<img src="~/Images/@product.Images.FirstOrDefault().ThumbNailName" alt="Product image">
			}
			else
			{
				<img src="~/Images/no-image.jpg" alt="Product image">
			}
			<div class="caption">
				<h3>@product.Name</h3>
				<h4>@product.Price €</h4>
				<p>@product.Description</p>
				<p style="height:10%; position:absolute; bottom:3px; margin: 0 10px;"><a href="@Url.Action("Buy", "Cart", new { id = product.Id })" class="btn btn-primary" role="button">To Cart</a> <a href="@Url.Action("Details", "Product", new { id = product.Id })" class="btn btn-default" role="button">Details</a></p>
			</div>
		</div>
	</div>
}





